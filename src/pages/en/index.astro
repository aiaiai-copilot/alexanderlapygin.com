---
import BaseLayout from "~/layouts/BaseLayout.astro";
import HomePage from "~/components/HomePage.astro";
import { t } from "~/i18n";

const locale = "en" as const;
const dict = t(locale);
---

<BaseLayout locale={locale} title={dict.nav.home}>
  <HomePage locale={locale} />
</BaseLayout>
