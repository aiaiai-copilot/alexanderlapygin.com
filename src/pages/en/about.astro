---
import BaseLayout from "~/layouts/BaseLayout.astro";
import AboutPage from "~/components/AboutPage.astro";
import { t } from "~/i18n";

const locale = "en" as const;
const dict = t(locale);
---

<BaseLayout locale={locale} title={dict.about.title}>
  <AboutPage locale={locale} />
</BaseLayout>
