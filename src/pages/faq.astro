---
import BaseLayout from "~/layouts/BaseLayout.astro";
import FaqPage from "~/components/FaqPage.astro";
import { t } from "~/i18n";

const locale = "ru" as const;
const dict = t(locale);
---

<BaseLayout locale={locale} title={dict.faq.title}>
  <FaqPage locale={locale} />
</BaseLayout>
