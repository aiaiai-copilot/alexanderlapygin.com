---
import BaseLayout from "~/layouts/BaseLayout.astro";
import ContactPage from "~/components/ContactPage.astro";
import { t } from "~/i18n";

const locale = "ru" as const;
const dict = t(locale);
---

<BaseLayout locale={locale} title={dict.contact.title}>
  <ContactPage locale={locale} />
</BaseLayout>
